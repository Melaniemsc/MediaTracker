<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Review</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Chakra+Petch:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Permanent+Marker&family=Poiret+One&family=Poller+One&display=swap" rel="stylesheet">
</head>

<body>
    <%- include('./partials/nav.ejs') %>
        <div class="general-div">
            <h1>Edit your review <%= user.username %>
            </h1>
            <% if (typeof message !== 'undefined') { %>
                <p class="error-message"><%= message %></p>
                <% } %>
            <div calss="editbuttons-display">
                <form action="/tracker/book/<%= book._id %>/reviews/<%= review.id %>/edit?_method=PUT" method="POST">
                    <textarea name="textEdit" id="textEdit" required><%= review.text %></textarea><br>
                    <button id="edit-button" type="submit">Submit</button>
                </form>

                <form action="/tracker/book/<%= book._id %>/reviews/<%= review.id %>/edit?_method=DELETE" method="POST"
                    onsubmit="return confirm('Are you sure you want to delete this review?');">
                    <button id="edit-button" type="submit">Delete Review</button>
                </form>
            </div>
        </div>
</body>

</html>