<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Review</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <div id="general-div">
        <h1>Edit your review <%= user.username %></h1>
<form action="/home/book/<%= book._id %>/reviews/<%= review.id %>/edit?_method=PUT" method="POST">
    <textarea name="textEdit" id="textEdit" required><%= review.text %></textarea><br>
    <button type="submit">Submit</button>
</form>

<form action="/home/book/<%= book._id %>/reviews/<%= review.id %>/edit?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this review?');">
    <button type="submit">Delete Review</button>
</form> 
    </div>
</body>
</html>